# X5 Marketing OS - Quick Start Guide

## Что было сделано (Фаза 1 - Критические исправления)

### ✅ Исправлена аутентификация на мобильных устройствах

**Проблема:** Google/Apple Sign-In не работал на iOS/Android
**Решение:** Добавлены callback handlers в [App.tsx](src/App.tsx:260-294)

Теперь Flutter может отправлять результат аутентификации в React:
- `window.onAppAuthSuccess(userData)` - успешная авторизация
- `window.onAppAuthFailed(error)` - ошибка авторизации

### ✅ API ключи перемещены в переменные окружения

**Проблема:** Секретные ключи были захардкожены в коде
**Решение:** Создана система переменных окружения

Изменённые файлы:
- [.env.example](.env.example) - шаблон для других разработчиков
- [.env.local](.env.local) - реальные ключи (НЕ коммитится!)
- [src/firebase.ts](src/firebase.ts) - использует `import.meta.env.VITE_*`
- [src/services/geminiService.ts](src/services/geminiService.ts) - использует `VITE_GEMINI_API_KEY`
- [vite.config.ts](vite.config.ts) - убран захардкоженный ключ

### ✅ Защищена база данных Firestore

**Проблема:** Правила `allow read, write: if true` - любой мог делать что угодно!
**Решение:** Новые строгие правила безопасности в [firestore.rules](firestore.rules)

Что защищено:
- Пользователи могут читать/писать только свои данные
- **КРИТИЧНО:** Нельзя увеличивать credits на клиенте (только уменьшать)
- Чаты доступны только участникам
- Курсы доступны для чтения всем, для записи только автору
- Системные настройки read-only

### ✅ Обновлён .gitignore

Добавлена защита от случайного коммита секретных файлов:
```
.env
.env.local
.env.production
```

---

## Установка и запуск

### Шаг 1: Установить зависимости

```bash
cd "d:\X\Adilkhan\5.3react"
npm install
```

### Шаг 2: Проверить переменные окружения

Файл `.env.local` уже создан с вашими текущими ключами. Если нужно изменить:

```bash
# Откройте .env.local в редакторе
notepad .env.local
```

**ВАЖНО:** Никогда не коммитьте `.env.local` в git!

### Шаг 3: Запустить приложение

Для разработки:
```bash
npm run dev
```

Для продакшн билда:
```bash
npm run build
```

### Шаг 4: Задеплоить новые Firestore rules

**КРИТИЧНО:** Без этого база данных остаётся незащищённой!

```bash
# Установить Firebase CLI (если ещё не установлен)
npm install -g firebase-tools

# Войти в Firebase
firebase login

# Выбрать проект
firebase use x5-marketing-app

# Задеплоить новые правила
firebase deploy --only firestore:rules
```

Проверка успешного деплоя:
1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `x5-marketing-app`
3. Firestore Database → Rules
4. Должны видеть новые правила с `function isAuthenticated()`

---

## Настройка мобильных приложений

Для работы Google/Apple Sign-In на iOS и Android:

📖 **Полная инструкция:** [FIREBASE_MOBILE_SETUP.md](FIREBASE_MOBILE_SETUP.md)

Краткая версия:

1. **Скачать конфигурационные файлы из Firebase Console:**
   - `google-services.json` → `d:\X\Adilkhan\5.4 flutter\android\app\`
   - `GoogleService-Info.plist` → `d:\X\Adilkhan\5.4 flutter\ios\Runner\`

2. **Получить SHA-1 сертификат для Android:**
   ```bash
   cd "d:\X\Adilkhan\5.4 flutter\android"
   gradlew signingReport
   ```
   Добавить в Firebase Console → Project Settings → Android app

3. **Настроить URL Schemes для iOS:**
   - Скопировать `REVERSED_CLIENT_ID` из `GoogleService-Info.plist`
   - Добавить в `Info.plist` в секцию `CFBundleURLSchemes`

4. **Включить провайдеры в Firebase:**
   - Authentication → Sign-in method
   - Enable: Google, Apple

5. **Протестировать:**
   ```bash
   cd "d:\X\Adilkhan\5.4 flutter"
   flutter run -d android  # или -d ios
   ```

---

## Проверка работоспособности

### Тест 1: Переменные окружения работают

```bash
npm run dev
```

Откройте DevTools → Console. НЕ должно быть ошибок типа:
- `VITE_GEMINI_API_KEY is not defined`
- `Cannot read property 'apiKey' of undefined`

### Тест 2: Firebase подключается

Откройте приложение → войдите в аккаунт. Проверьте:
1. Данные пользователя сохраняются в Firestore
2. Нет ошибок авторизации в консоли

### Тест 3: Firestore rules работают

Откройте DevTools → Console и попробуйте:

```javascript
// Попытка прочитать чужие данные (должна FAIL)
db.collection('users').doc('другой_пользователь_id').get()
  .then(() => console.log('ОШИБКА: Правила не работают!'))
  .catch(() => console.log('✅ Правила работают корректно'))

// Попытка увеличить свои credits (должна FAIL)
db.collection('users').doc('ваш_id').update({ credits: 9999 })
  .then(() => console.log('ОШИБКА: Можно накрутить credits!'))
  .catch(() => console.log('✅ Credits защищены'))
```

### Тест 4: Мобильная аутентификация (после настройки Firebase)

На телефоне:
1. Откройте приложение
2. Нажмите "Войти с Google" или "Войти с Apple"
3. После OAuth редиректа должны увидеть свой профиль
4. В Chrome DevTools (подключённый к WebView) должно появиться:
   ```
   [App] Received auth success from Flutter: {uid: "...", displayName: "...", ...}
   ```

---

## Структура проекта

```
d:\X\Adilkhan\5.3react\
├── .env.example              ← Шаблон переменных (коммитится)
├── .env.local                ← Реальные ключи (НЕ коммитится!)
├── .gitignore                ← Обновлён для защиты .env
├── firestore.rules           ← Новые правила безопасности
├── FIREBASE_MOBILE_SETUP.md  ← Инструкция для iOS/Android
├── QUICK_START.md            ← Этот файл
├── src/
│   ├── App.tsx               ← Добавлены auth callbacks (строки 260-294)
│   ├── firebase.ts           ← Использует переменные окружения
│   └── services/
│       └── geminiService.ts  ← Использует VITE_GEMINI_API_KEY
└── vite.config.ts            ← Убраны захардкоженные ключи
```

---

## Что дальше?

### Немедленно (обязательно):

1. ✅ **Задеплойте Firestore rules:**
   ```bash
   firebase deploy --only firestore:rules
   ```

2. ✅ **Настройте Firebase для мобилок:**
   - Следуйте [FIREBASE_MOBILE_SETUP.md](FIREBASE_MOBILE_SETUP.md)
   - Протестируйте Google/Apple Sign-In на реальном устройстве

3. ⚠️ **Проверьте что .env.local не в git:**
   ```bash
   git status
   # НЕ должно показывать .env.local
   ```

### Следующая фаза (Фаза 2 - Безопасность платежей):

Сейчас ещё есть проблема: **секретный ключ FreedomPay в коде!**

Необходимо:
1. Создать Cloud Functions для генерации платежей
2. Переместить `SECRET_KEY` на сервер
3. Добавить серверную валидацию credits

📖 См. полный план в [plan файле](C:\Users\tuako\.claude\plans\shiny-inventing-crane.md)

---

## Полезные команды

```bash
# Разработка
npm run dev

# Продакшн билд
npm run build

# Превью билда
npm run preview

# Проверить что есть в dist
npm run build && ls dist

# Очистить кэш и пересобрать
rm -rf node_modules dist
npm install
npm run build

# Firebase deploy (только Firestore rules)
firebase deploy --only firestore:rules

# Firebase deploy (всё)
firebase deploy

# Flutter mobile app
cd "../5.4 flutter"
flutter clean
flutter pub get
flutter run -d android
```

---

## Troubleshooting

### Проблема: "VITE_GEMINI_API_KEY is not defined"

**Решение:**
1. Проверьте что файл `.env.local` существует
2. Проверьте что в нём есть `VITE_GEMINI_API_KEY=...`
3. Перезапустите dev server: `Ctrl+C` → `npm run dev`

### Проблема: Firebase connection errors

**Решение:**
1. Проверьте `.env.local` - все VITE_FIREBASE_* переменные заполнены
2. Проверьте интернет подключение
3. Проверьте Firebase Console - проект `x5-marketing-app` существует

### Проблема: Google Sign-In не работает на Android

**Решение:**
1. Проверьте что добавили SHA-1 в Firebase Console
2. Скачали обновлённый `google-services.json`
3. Выполните `flutter clean && flutter run`

Полный troubleshooting: [FIREBASE_MOBILE_SETUP.md](FIREBASE_MOBILE_SETUP.md)

---

## Контакты и поддержка

- **GitHub Issues:** (если есть репозиторий)
- **Firebase Console:** https://console.firebase.google.com/
- **План улучшений:** `C:\Users\tuako\.claude\plans\shiny-inventing-crane.md`

---

**Статус:** ✅ Фаза 1 завершена
**Следующая фаза:** Безопасность платежей (Cloud Functions)
**Дата обновления:** 2026-01-20
